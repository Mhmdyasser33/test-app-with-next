// to convert from server component to client component we should use this
"use client" ;
import React , {useEffect , useState} from 'react'
const Todo = () => {
/*     const [date , setDate] = useState([]);
    useEffect(async() =>{
 const result = await fetch("https://jsonplaceholder.typicode.com/todos") ;
 const data = await result.json() ;
 setDate(data) ;
    } , []) */
  return (
      <>
     {/*  <div>Title List : </div>
      {date?.map((item) =>(

        <div>{item.title}</div>
      ))} */}
     </>
  )
}

export default Todo